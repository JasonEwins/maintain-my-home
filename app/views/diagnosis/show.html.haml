= simple_form_for :diagnosis, url: request.fullpath do |f|
  %fieldset
    %legend.question
      %h1= @question.question

      .answer
        = f.input :answer,
          as: :radio_buttons,
          collection: @question.answers,
          label: false

        = f.button :submit
